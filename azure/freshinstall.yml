---
	hosts: azure
	Tasks:
	
	- name: update cache
	apt: update_cache
	sudo: yes

	- name: update programs
	command: apt-get dist-upgrade -y
	sudo: yes

	-name: Install important apps
	apt: name: "{{ item }}"
	state: present
	with_items:
		  - git
		  - wget
		  - nano
		  - 
